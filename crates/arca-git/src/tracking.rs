//! 追踪冲突检测与 vault 一致性检查（spec §4.3.2 处置表）。
//!
//! - 孤儿数据集（有 `.arca/` 但不在注册表）→ 报告并拒绝同步，等待显式 `arca register`；
//! - `hub_instance_id` 与注册表不符 → 拒绝同步（防误绑）；
//! - 数据集嵌套 / 同一路径登记两次 → 拒绝；
//! - 已被 git 追踪的文件落入数据集（`.gitignore` 对已追踪文件无效）→ 检出并报告。
//!
//! TODO(M1)：检查项枚举与 doctor 报告结构。
