[package]
name = "arca-chunk"
description = "内容寻址原语：FastCDC 分块 + BLAKE3 哈希 + zstd 压缩"
license = "Apache-2.0"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
homepage.workspace = true

[dependencies]
# 计划依赖（实现时启用）：blake3, fastcdc, zstd
