//! 分级驻留与水化调度（spec §4.8、§6.1）——防 hydration 风暴（头号体验风险）。
//!
//! - 体积 ≤ 阈值（默认 8 MiB）→ 始终本地驻留，永不 dehydrate（笔记渲染零延迟）；
//! - 大文件 → 占位符，打开即取；热度预取（最近笔记同目录附件，默认 14 天）；
//! - pin / LRU 释放（三态：online-only / locally-available / pinned）；
//! - 区分元数据读与内容读；水化并发上限 + 批量合并 + 向 OS 报告进度；
//! - **必测**：全库索引水化字节数 = 0（§6.3 第 7 条）。
//!
//! TODO(M3)：策略引擎、水化队列。
