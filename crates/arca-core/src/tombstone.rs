//! 删除与改名传播：tombstone 语义与四道闸门（I3：同步路径无销毁权）。
//!
//! - 本地删除 → hub 记 tombstone，数据入 `.arca/trash/` 保留期（默认 180 天）；
//! - 远端 tombstone 到达 client：四道闸门（read_roots 范围 → 单点确认 → 基线一致性）
//!   通过后仅移除本地副本/占位符；本地有未同步修改 → 升级为冲突副本，不删；
//! - server 角色：数据只移入本地 trash，任何云侧语义都不缩减 server 副本（§4.7）;
//! - 改名/移动：身份不动、映射搬家（三步原子改名，I7）；
//!   跨数据集移动 = 源记 tombstone + 目标新纳管（§5.3）。
//!
//! 物理销毁只经显式 GC（arcad / CLI 侧实现，此处只定义语义与决策）。
//!
//! TODO(M2)：闸门决策逻辑、保留期模型。
