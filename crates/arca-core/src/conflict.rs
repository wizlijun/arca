//! 结构化冲突：双版本并存 + 结构化上报（绝不合并二进制，spec §1.2、§5.3）。
//!
//! - CAS 412 → 结构化冲突 `{base, theirs, yours}`；
//! - 本地内容落地为冲突副本（新身份），命名对齐 Dropbox 惯例：
//!   `原名 (设备名 的冲突副本 2026-08-04).ext`；
//! - 同时产出结构化冲突事件供 agent / UI 消费（`vault_conflicts`，§10.1）。
//!
//! TODO(M2)：冲突类型、命名生成、事件结构。
