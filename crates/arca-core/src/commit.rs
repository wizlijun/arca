//! CAS 提交状态机（I4：一切写入走 CAS）。
//!
//! 提交必须携带父版本哈希（If-Match 语义；`absent` 表示仅创建），
//! 过期即拒绝（412），绝不静默覆盖。CAS 失败产生的分叉以冲突副本
//! （新身份）落地，不在版本链内（spec §4.1、§5.1）。
//!
//! 上传侧配套：幂等五元组、断点续传会话、丢失 commit 的 no-op 恢复、
//! 两轮稳定性签名防抖（继承 lazync §7–§8，spec §5.4）。
//!
//! TODO(M1)：提交状态机、file:// 直连的本地提交路径（tmp → fsync → rename + .txn）。
